# Copy this file to .env and fill in real values. .env is gitignored -- never commit it.

# --- Required for the API to start ---
MODEL_DIR=models
DATABASE_URL=sqlite:///data/processed/reports.db
CHROMA_PERSIST_DIR=data/processed/chroma_index
ENV=development

# --- Only required for the /ask (RAG) endpoint, Ticket 11 -- not needed until then ---
# Free tier: https://console.groq.com
LLM_API_KEY=

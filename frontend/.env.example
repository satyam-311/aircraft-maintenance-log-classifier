# Copy to .env for local dev if you want to point at a deployed backend instead of
# the Vite dev proxy. Leave unset for normal local development (uses /api proxy to
# localhost:8000, configured in vite.config.js).
VITE_API_BASE_URL=
